#!/usr/bin/env sh
# Overseer Kit CLI — dispatches init|sync|status to the Python runtime (K4b).
set -eu
ROOT="$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)"
export PYTHONPATH="${ROOT}${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m cli.main "$@"
